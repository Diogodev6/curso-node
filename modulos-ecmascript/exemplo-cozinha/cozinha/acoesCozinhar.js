const cozinhar = (alimento) => `Cozinhando ${alimento}`

const assar = (alimento) => `Assando ${alimento}`

const ferver = (alimento) => `Fervendo ${alimento}`

const cortar = (alimento) => `Cortando ${alimento}`

const servir = (alimento) => `Servindo ${alimento}`

export default { cozinhar, assar, ferver, cortar, servir }